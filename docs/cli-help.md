# `clawee-release-manage` command reference

Generated by `clawee-release-manage docs`. Do not edit — regenerate with:

```sh
go run ./cmd/clawee-release-manage docs > docs/cli-help.md
```

## `clawee-release-manage`

the publish-management service for the Clawee release channel

```
clawee-release-manage — the publish-management service for the Clawee release channel

Usage:
  clawee-release-manage <verb> [flags]

Commands:
  admin                         operator accounts for the manage surface
    add                         provision an account; the second factor enrols at first login
            <name>              the account name, as it is typed at the login page
            [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)
            [--password-stdin]  read the password from standard input instead of prompting
    list                        print the accounts and whether each has enrolled a second factor
            [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)
    remove                      delete an account and end its sessions
            <name>              the account to delete
            [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)
  version                       print the build stamp, and the applied migrations when a data dir is
                                named
            [--data-dir <dir>]  the dir holding the catalog; when given, the applied migrations are
                                printed too
  docs                          print the whole command surface as markdown

Run 'clawee-release-manage <verb> --help' for that command's help.
```

### `clawee-release-manage admin`

operator accounts for the manage surface

```
clawee-release-manage admin — operator accounts for the manage surface

Usage:
  clawee-release-manage admin <verb> [flags]

Commands:
  admin                         operator accounts for the manage surface
    add                         provision an account; the second factor enrols at first login
            <name>              the account name, as it is typed at the login page
            [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)
            [--password-stdin]  read the password from standard input instead of prompting
    list                        print the accounts and whether each has enrolled a second factor
            [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)
    remove                      delete an account and end its sessions
            <name>              the account to delete
            [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)

Run 'clawee-release-manage admin <verb> --help' for that command's help.
```

#### `clawee-release-manage admin add`

provision an account; the second factor enrols at first login

```
clawee-release-manage admin add — provision an account; the second factor enrols at first login

Usage:
  clawee-release-manage admin add <name> [flags]

Commands:
  add                      provision an account; the second factor enrols at first login
       <name>              the account name, as it is typed at the login page
       [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)
       [--password-stdin]  read the password from standard input instead of prompting
```

#### `clawee-release-manage admin list`

print the accounts and whether each has enrolled a second factor

```
clawee-release-manage admin list — print the accounts and whether each has enrolled a second factor

Usage:
  clawee-release-manage admin list [flags]

Commands:
  list                      print the accounts and whether each has enrolled a second factor
        [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)
```

#### `clawee-release-manage admin remove`

delete an account and end its sessions

```
clawee-release-manage admin remove — delete an account and end its sessions

Usage:
  clawee-release-manage admin remove <name> [flags]

Commands:
  remove                      delete an account and end its sessions
          <name>              the account to delete
          [--data-dir <dir>]  the dir holding the catalog and the service secret key (required)
```

### `clawee-release-manage version`

print the build stamp, and the applied migrations when a data dir is named

```
clawee-release-manage version — print the build stamp, and the applied migrations when a data dir is named

Usage:
  clawee-release-manage version [flags]

Commands:
  version                      print the build stamp, and the applied migrations when a data dir is
                               named
           [--data-dir <dir>]  the dir holding the catalog; when given, the applied migrations are
                               printed too
```

### `clawee-release-manage docs`

print the whole command surface as markdown

```
clawee-release-manage docs — print the whole command surface as markdown

Usage:
  clawee-release-manage docs

Commands:
  docs    print the whole command surface as markdown
```

